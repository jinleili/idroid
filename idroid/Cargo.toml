[package]
name = "idroid"
version = "0.2.5"
authors = ["li jinlei <grenlight@icloud.com>"]
edition = "2018"

[lib]
name = "idroid"
crate-type = ["rlib", "staticlib"]
# [target.'cfg(target_os = "android")'.lib]
# crate-type = ["cdylib"]

[dependencies]
libc = "*"
wgpu = { git = "https://github.com/gfx-rs/wgpu-rs",  version = "0.6", rev = "1f7716e22ef700508795c4a7e65b4e57da6dfddb"  }
# wgpu = { path = "../../../wgpu-rs" }
uni-view = { path="../uni-view" }

nalgebra-glm = "*"
image = "0.23"
lazy_static = "*"
zerocopy = "0.3"
futures = "0.3"

[target.'cfg(any(target_os = "macos", target_os = "windows", target_os = "linux"))'.dependencies]
shaderc = { path="../../shaderc-rs/shaderc-rs" }
objc = "*"
winit = { version = "0.22.2", features = ["web-sys"] }
raw-window-handle = "0.3"

[build-dependencies]
shaderc = { path="../../shaderc-rs/shaderc-rs" }

